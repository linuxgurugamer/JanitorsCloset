KERBALCHANGELOG
{
    showChangelog = True
    modName = Janitors Closet
    license = CC-BY-NC-4.0
    author = Linuxgurugamer

    VERSION
    {
        version = 0.0.1
        CHANGE
        {
            change = Initial release
            type = add
        }
    }
    VERSION
    {
        version = 0.0.2	
        CHANGE
        {
            change = Added ChangeLog
           type = add
        }
        CHANGE
        {
 		    change = Fixed import so it will be added to current list
            type = fix
        }
    }
    VERSION
    {
        version = 0.0.3	
        CHANGE
        {
            change = Fixed problem when you permapruned some parts, and then tried to do some more
		    change = Fixed a couple of exceptions when parts aren't properly defined
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.0.4	
        CHANGE
        {
            change = Fixed clickthrough issue, thanks @PapaJoesSoup
		    change = Fixed title of import screen
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.0.5	
        CHANGE
        {
            change = Added code to check at game startup to see if files had been restored somehow.  Usually would occur as a result of Steam updating files, or mod being updated.  Assumption is that if the file exists in an unpruned state, it is newer than the pruned file and will replace the pruned file
            type = add
        }
    }
 
   VERSION
    {
        version = 0.1.1	
        CHANGE
        {
            change = Official release for 1.2
            type = add
        }
    }
 
   VERSION
    {
        version = 0.1.2	
        CHANGE
        {
            change = Fixed bug with PermaPrune, where it was pruning models and meshes which were used by non-pruned parts
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.1.3	
        CHANGE
        {
            change = Fixed bug where right-clicks were spawning parts in the editor
            type = fix
        }
        CHANGE
        {
            change = Added code to update asssembly info with version
            type = add
        }
    }
 
   VERSION
    {
        version = 0.1.4
		
        CHANGE
        {
            change = Added window to filter mod parts.  Most of the new code is from an old mod:  Simple Part Organizer
            type = add
        }
    }
 
   VERSION
    {
        version = 0.1.5
		
        CHANGE
        {
            change = Added filter by size
            type = add
        }
    }
 
   VERSION
    {
        version = 0.1.6

        CHANGE
        {
            change = Fixed issue when same model file referenced both as a mesh and a model, too many files were being pruned.  Replaced some rather messy code with a more universal way of comparing files, by using the partURL everywhere
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.1.7
		
        CHANGE
        {
            change = Fixed sort of sizes
		    change = Fixed sort so that:
			    subchange = The line with "larger" appears at the end
			    subchange = Lines beginning with "Adapter" appear after the other lines
            type = fix
        }
        CHANGE
        {
		    change = Added text "Adapter" to adapter sizes
		    change = Moved initial window position right 100 pixels
		    change = Added code so that window will have a minimum height of 10 buttons, and a maximum height of 20
            type = add
        }
    }
 
   VERSION
    {
        version = 0.1.8
		Removed limit
        CHANGE
        {
            change = Removed limitation on width of displayable lines in the mod filter
		    change = Added check for null part  in the InitialPartScan(), needed in case other mods mess up during loading and put a null part into the loadedParts list
            type = add
        }
    }
 
   VERSION
    {
        version = 0.2.0
		
        CHANGE
        {
            change = Initial beta release for Janitor's Toolbar
            type = add
        }
    }
 
   VERSION
    {
        version = 0.2.1
		
        CHANGE
        {
            change = Added check for no more available folders
		    change = Added check for compatible version of Texture Replacer
            type = add
        }
    }
 
   VERSION
    {
        version = 0.2.2
		
        CHANGE
        {
            change = Fixed popup and autohide of toolbar
		    change = Fixed issue where some buttons were causing odd behaviour
		    change = Removed log spam from mod filter 
		    change = Possible fix for blank line in Mod filtyer list
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.2.3
		
        CHANGE
        {
            change = Fixed popups going away too fast
		    change = Fixed hiding
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.2.4
		
        CHANGE
        {
            change = Fixed problem with buttons which had right-clicks responses
		    change = Removed some more log spam
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.2.5
		
        CHANGE
        {
            change = Added Settings page
			subchange = Toolbar Functionality enabled/disabled
			subchange = enable/disable Menu popup on mouseover for toolbar button folders
			subchange = Enable/disable popup menu in Editor
			subchange = Configurable time for hover menus
            type = add
        }
        CHANGE
        {
		    change = Added new button to Show Blocked window, "Unblock All"
            type = add
        }

        CHANGE
        {
			change = If popup menu in editor is disabled, then right-click on button brings it up
		    change = Removed the Unblock All from the popup menu
		    change = Fixed bug on "Show Unblock" window, where individual item could not be unblocked
		    change = Changed display from internal partname to the parttitle on the "Show Blocked" window
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.2.6
		
        CHANGE
        {
		    change = fixed issue where if editor was exited with the mod filter window showing, it stayed showing
		    change = Fixed issue where if menu popup was showing when exiting editor, it would be reshown when reentering
            type = fix
        }
        CHANGE
        {
            change = Added 10 buttons to Modfilter window, to save settings
            type = add
        }
    }
 
   VERSION
    {
        version = 0.2.7
		
        CHANGE
        {
            change = Fixed issue with Mapview not showing button folders
		    change = Fixed issue with mapview and mods which show their buttons on the mapview only (ie:  PreciseManeuver)
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.2.8
		
        CHANGE
        {
            change = Fixed issue with part names having spaces, weren't being allowed to be seen 
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.2.9
		
        CHANGE
        {
            change = Fixed problem with hidden buttons not being displayed
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.2.10
		
        CHANGE
        {
            change = Added saving of hidden buttons
            type = add
        }
    }
 
   VERSION
    {
        version = 0.2.11
		
        CHANGE
        {
            change = Rewrote internal button handling code to solve problems with some mods (specifically USI-LS)
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.2.11.1
		
        CHANGE
        {
            change = corrected using the correct value for the gamescene
		    change = Fixed delay after changing scenes
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.2.11.2
		
        CHANGE
        {
            change = Added option to disable hover on icons in JC toolbar
            type = add
        }
    }
 
   VERSION
    {
        version = 0.2.11.3
		
        CHANGE
        {
            change = Fixed display of hidden buttons 
		    change = Updated some of the logging to only log errors in the release version
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.2.11.4

        CHANGE
        {
            change = Fixed nullref when no toolbars defined, this happend when changing scenes
            type = fix
        }
		
        CHANGE
        {
		    change = Added checks for empty file name when saving & loading blacklist file
		    change = Added Debug mode (spams log file)
            type = add
        }
    }
 
   VERSION
    {
        version = 0.2.11.5
		
        CHANGE
        {
            change = Added a few checks to avoid a nullref
            type = add
        }
    }
 
   VERSION
    {
        version = 0.2.11.6
		
        CHANGE
        {
            change = Fixed a rare null-ref related to the KerboKatz mods
            type = fix
        }
        CHANGE
        {
    		change = Added config file of blacklisted icons, currently only has Kerbokatz
            type = add
        }
    }
 
   VERSION
    {
        version = 0.2.11.7
		
        CHANGE
        {
            change = fixed issue where lists weren't saved when removing a button from a folder
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.2.11.8
		
        CHANGE
        {
            change = Removed call to close windows when changing scenes.
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.2.11.9
		
        CHANGE
        {
            change = Added menu item to blacklist a button and save it
            type = add
        }
    }
 
   VERSION
    {
        version = 0.2.12
		
        CHANGE
        {
            change = Fixed issue with SEP where hovering over a button caused flickering
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.2.12.1
		
        CHANGE
        {
            change = Fixed menu size to adjust for the blacklist option
            type = fix
        }
        CHANGE
        {
		    change = Added dialog to confirm blacklisting button
            type = add
        }
    }
 
   VERSION
    {
        version = 0.2.13
		
        CHANGE
        {
            change = Fixed problem unhiding a hidden button
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.2.14
		
        CHANGE
        {
            change = Fixed problem with quickload and scene changes not properly resetting toolbar
            type = fix
        }
        CHANGE
        {
		    change = Added Identify menu item to be able to specify an identity for a button
		    change = Added tooltips for buttons in the JCToolbar, for those buttons which have been identified
            type = add
        }
    }
 
   VERSION
    {
        version = 0.2.14.1
		
        CHANGE
        {
            change = Fixed nullrefs (harmless) when no buttonidentifiers are found for a hovered-over button
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.2.14.2
		
        CHANGE
        {
            change = Really fixed those nullrefs now
		    change = Fixed harmelss nullref at startup when there were a lot of mods
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.2.14.3
		
        CHANGE
        {
            change = Fixed nullref when identifying buttons
		    change = Added many identifiers to default identifier file
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.2.14.4
		
        CHANGE
        {
            change = Fix for tooltips being duplicated
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.2.14.5
		
        CHANGE
        {
            change = just a rebuild
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.2.14.6
		
        CHANGE
        {
            change = Fixed the nullref which returned from the 0.2.14.1 fix
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.2.15
		
        CHANGE
        {
            change = Added color to Mod Filter menu item when mods are filterd
		    change = Added count of filtered sizes/mods to Mod Fitler menu item
            type = add
        }
    }
 
   VERSION
    {
        version = 0.2.15.6
		
        CHANGE
        {
            change = Fixed memory leak of multi megs a sec when the toolbar was displayed, by limiting the updating of the toolbar icons to once every 5 seconds
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.2.16
		
        CHANGE
        {
            change = Permanent fix to memory leak problem
		    change = Fixed performance issue as well, where fps would drop by 20-40fps when toolbar was open
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.2.17
		
        CHANGE
        {
            change = Found error in CRC calculations (was making a fixed texture of 38x38, instead of using the source textures size).  This was causing some textures to come up with the same CRC, which breaks things
		    change = Added try/catch to watch for duplicate CRC codes
            type = fix
        }
    }

   VERSION
    {
        version = 0.2.17.1
		
        CHANGE
        {
            change = Removed horizontal scroll bar in ModFilterWindow
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.2.18
		
        CHANGE
        {
            change = Fixed bug where the blocked part titles weren't being shown in the editor after leaving the editor and returning
            type = fix
        }
        CHANGE
        {
		    change = Added HideUnpurchased button to Mod Filter window
            type = add
        }
    }
 
   VERSION
    {
        version = 0.2.19
		
        CHANGE
        {
            change = Added check when loading blacklist to be sure part isn't pruned, avoids nullref
            type = add
        }
    }
 
   VERSION
    {
        version = 0.2.20
		
        CHANGE
        {
            change = Fixed nullref when hidden icon textures were null
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.3.0
		
        CHANGE
        {
            change = Replaced depreciated call to GameEvents.onLevelWasLoaded with Unity SceneManagment calls
		    change = Moved search filter initialization into coroutine because of 1.3 change
		    change = Replaced Input.GetKey with ExtendedInput.GetKey
		    change = Updated for 1.3
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.3.1
		
        CHANGE
        {
            change = Fixed bug where parts would be permenantly hidden if one or more filters were defined in the modfilter screen.  
            type = add
        }
        CHANGE
        {
		    change = Added filtering by resource
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.3.2
		
        CHANGE
        {
            change = Add tooltip to show mod part is from when hovering over part in VAB partlist.  
		    change = Added settings for mod tooltip.  If disabled, then tooltip will be shown when modifier key is held down
		    change = updated build script (apparently last version wasn't released properly)
            type = add
        }
    }
 
   VERSION
    {
        version = 0.3.3
		
        CHANGE
        {
            change = Updated for KSP 1.3.1
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.3.4
		
        CHANGE
        {
            change = Updated for 1.4.1
		    change = Added ClickThroughBlocker support
		    change = Added Jenkins config
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.3.4.1
	
        CHANGE
        {
            change = Updated .version for all of 1.4
	        change = Added dependency checking for ClickThroughBlocker
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.3.4.2
	
        CHANGE
        {
            change = Version bump for 1.5 rebuild
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.3.4.3
	
        CHANGE
        {
            change = Added code to detect the missing kerbals:  kerbalEVAfemale, kerbalEVAVintage, kerbalEVAfemaleVintage
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.3.4.4
	
        CHANGE
        {
            change = Replaced individual checks for each of the kerbal parts with a single one using Contains
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.3.4.5
	
        CHANGE
        {
            change = version bump to fix spacedock
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.3.4.6
	
        CHANGE
        {
            change = Added new size 1.5 (1.875m) to filter selection
            type = add
        }
    }
 
   VERSION
    {
        version = 0.3.4.7
	
        CHANGE
        {
            change = Fixed incorrect initialization of constants in ModFilterWindow
            type = fix
        }
        CHANGE
        {
	        change = Added check for null in GetButtonTexture
            type = add
        }
    }
 
   VERSION
    {
        version = 0.3.4.8
	
        CHANGE
        {
            change = Added some more checks for null refs
            type = fix
        }
    }
 
   VERSION
    {
        version = 0.3.5
	
        CHANGE
        {
            change = Fixed resource filter to also filter on propellents used by engines & RCS
	        change = Optimized filters a bit
	        change = Fixed the "Reset All" button to properly reset the Resources filters
	        change = Fixed inability to save Mod Filter Window settings properly
            type = fix
        }
        CHANGE
        {
	        change = Added an inverse filter, to exclude specific mods/ size/resources
	        change = Added help screen to mod filter window
	        change = Added PartModule filter
	        change = Added Module Blacklist (ignores modules in the blacklist)
	        change = Added ReadableNames list
            type = add
        }
    }
 
   VERSION
    {
        version = 0.3.5.1
	
        CHANGE
        {
            change = Fixed Missing Key error when resources were listd in the filterconfig file and not available
	        change = Adjusted the sort so that "None" is always first
            type = fix
        }
        CHANGE
        {
	        change = Added "None" to modules (for those parts which didn't have any modules)
            type = add
        }
    }
 
   VERSION
    {
        version = 0.3.6
	
        CHANGE
        {
            change = Fixed nullref when returning to the main menu (new in 1.7.3)
	        change = Added InstallChecker
	        change = Updated AssemblyVersion.tt
            type = fix
        }
    }
 
    VERSION
    {
        version = 0.3.6.1
	
        CHANGE
        {
            change = Fixed another nullref when returning to the main menu, was happening in 1.7.2
            type = fix
        }
    }
 
    VERSION
    {
        version = 0.3.7
	
        CHANGE
        {
            change = Updated for KSP 1.8
	        change = Updated some old constants
            type = fix
        }
    }
 
    VERSION
    {
        version = 0.3.7.1
	
        CHANGE
        {
	        change = Changed non-static vars to static to fix nullrefs
		        subchange = origBackgroundColor;
		        subchange = styleButtonLeftAligned;
	        change = Revamped part sizes to use bulkhead profiles instead of node sizes
	        change = Made initialization static to minimize entry time into the editor after the first time
            type = fix
        }
        CHANGE
        {
            change = Added check for "None" in ModFilterWindow 
	        change = Added new section to FiltersConfig.cfg for all known bulkheadProfiles
	        change = Improved sort of entries in filter window
	        change = Added "-v2" to the name of the JCModfilter file names
	        change = Added saving of window positions in the Editor

        	change = New Dependency: SpaceTuxLibrary
            type = add
        }
    }
 

   VERSION
    {
        version = 0.3.7.2 
	
        CHANGE
        {
            change = Removed unnecessary message about missing WinPos.cfg file
            type = Fix
        }
    }
 

   VERSION
    {
        version = 0.3.7.3
	
        CHANGE
        {
            change = Added check for part already added.  PlumeParty has a part: pp.vvmach, which apparently has been included in other mods (ChrayolDesign is one) and was causing errors because of duplicate parts
            type = add
        }
    }
 

   VERSION
    {
        version = 0.3.7.4
	
        CHANGE
        {
            change = Fixed Nullref when moving cursor over BG icon inventory spaces which didn't have any parts in them, also fixes nullref when trying to pull the breaking ground parts, and they don't pulled into the container
            type = fix
        }
    }
 

   VERSION
    {
        version = 0.3.7.5
	
        CHANGE
        {
            change = Fixed stock toolbar button coming back after being disabled
	        change = Removed need to restart after changing toolbar button option
	        change = Updated description and added tooltip to toolbar button option
            type = fix
        }
    }

   VERSION
    {
        version = 0.3.7.6
	
        CHANGE
        {
            change = Fixed button being removed in the editor when disabled in other scenes
            type = fix
        }
    }

   VERSION
    {
        version = 0.3.7.7
	
        CHANGE
        {
            change = Fixed error when invalid entries are in a bulkheadProfiles (ie: missing commas)
            type = fix
        }
    }

   VERSION
    {
        version = 0.3.7.8
	
        CHANGE
        {
            change = Added AssemblyFileVersion
	        change = Updated version file for 1.12
            type = fix
        }
    }

   VERSION
    {
        version = 0.3.7.9
	
        CHANGE
        {
            change = Replaced LINQ ToDictionary with simple foreach loop (fixes some nullrefs)
	        change = Added check for existing key in addToHiddenBlockList() (fixes some nullrefs)
            type = fix
        }
    }

   VERSION
    {
        version = 0.3.8
	
        CHANGE
        {
            change = Cleaned up most (all) of debugging error messages showing duplicate buttons, etc
	        change = Replaced all unnecessary type/catch which were simply reporting if an entry was already in a list, replaced with simple indexing
            type = fix
        }
    }

   VERSION
    {
        version = 0.3.9
        CHANGE
        {
            change = The perma-prune button now updates the CKAN installation filters from Global install filters if a CKAN folder is found in the game root, which will tell CKAN that it should not consider perma-pruned files as missing because the user chose to remove them. This will prevent CKAN v1.35.0 and later from prompting the user to reinstall the mods that the pruned files came from.
            type = add
        }
    } 

   VERSION
    {
        version = 0.4.0
        CHANGE
        {
            change = Fix toolbar sync performance and add editor-only option
		    change = When parts are PermaPruned, texture files are now also renamed (the game will not load them the next time it's launched, saving memory). In addition to textures directly linked to part models, textures specified in the part's config are also renamed
		    change = Displays the number of parts in the game's database, as well as the number and total size of their models and textures;
		    change = Displays the number and total size of renamed files.
            type = add
        }
    }
 

    VERSION
    {
        version = 0.4.1	
        CHANGE
        {
            change = The icons for the Application toolbar buttons can now scale with GameSettings.UI_Scale
            type = add
        }
    }
    VERSION
    {
        version = 0.4.1.1
        CHANGE
        {
            change = Added support for Kerbal Changelog
            type = add
        }
    }
    VERSION
    {
        version = 0.4.1.2
        CHANGE
        {
            change = Fixed bug with folders not showing contents introduced by previous patch
            type = fix
        }
    } 
 }